# Models Directory

This folder is for cleaned or runtime-ready imported models.

Keep imports here when they are ready to be validated in the target engine/runtime.

Supported formats:
- .fbx
- .gltf
- .glb
- .obj

This directory replaces the legacy generated map output convention.
